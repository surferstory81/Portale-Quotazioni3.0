frontend:
  image: frontend:latest
  replicaCount: 1
  ingress:
    enabled: true
    host: portale.local
backend:
  image: backend:latest
  replicaCount: 1
  ingress:
    enabled: true
    host: api.portale.local
  jwtSecret: change-me-to-a-strong-secret-at-least-32-chars
  adminDefaultPassword: Admin@2024!
  smtpPass: "mylv bult dioj ibro"
db:
  image: postgres:15-alpine
  storage: 5Gi
  user: portale_user
  password: CHANGE_ME_DB_PASSWORD
  db: portaledb
